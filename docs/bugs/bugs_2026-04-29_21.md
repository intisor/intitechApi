# Bug Documentation Timeline (bugs_2026-04-29_21.md)

Append-only bug audit trail generated by CodeNarrator.

<!-- bug-entry:diagnostic:Programcs_L8_iservicecollection-does-not-contain_mokkhj35 -->
    ## Bug Report: `Programcs_L8_iservicecollection-does-not-contain_mokkhj35`

| Field | Value |
|---|---|
| **File** | `Program.cs` |
| **Line** | 8 (col 18) |
| **Source** | unknown |
| **Severity** | 🔴 Error |
| **Detected** | 2026-04-29 21:28:39 UTC |
| **Resolved** | 2026-04-29 21:28:50 UTC |
| **Time to Fix** | 11s |
| **Status** | ✅ Resolved |

---

## Error Message

```
'IServiceCollection' does not contain a definition for 'AddOpenApi' and no accessible extension method 'AddOpenApi' accepting a first argument of type 'IServiceCollection' could be found (are you missing a using directive or an assembly reference?)
```

---

## Code Context

```
   5: // Services
   6: builder.Services.AddMemoryCache();
   7: builder.Services.AddHttpClient<GitHubService>();
   8: builder.Services.AddOpenApi(); ◄
   9: 
  10: // CORS — only allow portfolio origin
  11: builder.Services.AddCors(options =>
```

---

## Predicted Fix *(CodeNarrator — before any fix was applied)*

> _No language model available._

---

## Actual Fix Applied

```diff
diff --git a/IntitechApi.csproj b/IntitechApi.csproj
index 9420abc..003969e 100644
--- a/IntitechApi.csproj
+++ b/IntitechApi.csproj
@@ -1,14 +1,14 @@
 <Project Sdk="Microsoft.NET.Sdk.Web">
 
   <PropertyGroup>
-    <TargetFramework>net9.0</TargetFramework>
+    <TargetFramework>net10.0</TargetFramework>
     <Nullable>enable</Nullable>
     <ImplicitUsings>enable</ImplicitUsings>
     <RootNamespace>IntitechApi</RootNamespace>
   </PropertyGroup>
 
   <ItemGroup>
-    <PackageReference Include="Microsoft.AspNetCore.OpenApi" Version="9.0.0" />
+    <PackageReference Include="Microsoft.AspNetCore.OpenApi" Version="10.0.0" />
   </ItemGroup>
 
 </Project>
diff --git a/Services/GitHubService.cs b/Services/GitHubService.cs
index 245a42a..4444d05 100644
--- a/Services/GitHubService.cs
+++ b/Services/GitHubService.cs
@@ -12,7 +12,7 @@ public class GitHubService(
     IConfiguration config,
     ILogger<GitHubService> logger)
 {
-    private readonly string _username = config["GitHub:Username"] ?? "intitech";
+    private readonly string _username = config["GitHub:Username"] ?? "intisor";
     private readonly string? _token = config["GitHub:Token"];
 
     private void SetAuthHeaders()
diff --git a/appsettings.json b/appsettings.json
index ba89833..0462495 100644
--- a/appsettings.json
+++ b/appsettings.json
@@ -7,7 +7,7 @@
   },
   "AllowedHosts": "*",
   "GitHub": {
-    "Username": "intitech",
+    "Username": "intisor",
     "Token": ""
   }
 }

```

---

## Validation

> _No language model available for validation._

---

*Generated by [CodeNarrator](https://github.com/codenarrator/codebase-narrator) · 2026-04-29 21:28:39 UTC*

<!-- bug-entry:diagnostic:Programcs_L32_webapplication-does-not-contain_mokkhj35 -->
    ## Bug Report: `Programcs_L32_webapplication-does-not-contain_mokkhj35`

| Field | Value |
|---|---|
| **File** | `Program.cs` |
| **Line** | 32 (col 9) |
| **Source** | unknown |
| **Severity** | 🔴 Error |
| **Detected** | 2026-04-29 21:28:39 UTC |
| **Resolved** | 2026-04-29 21:28:51 UTC |
| **Time to Fix** | 12s |
| **Status** | ✅ Resolved |

---

## Error Message

```
'WebApplication' does not contain a definition for 'MapOpenApi' and no accessible extension method 'MapOpenApi' accepting a first argument of type 'WebApplication' could be found (are you missing a using directive or an assembly reference?)
```

---

## Code Context

```
  29: app.UseCors("PortfolioOnly");
  30: 
  31: if (app.Environment.IsDevelopment())
  32:     app.MapOpenApi(); ◄
  33: 
  34: // ── Health ────────────────────────────────────────────────────────────────────
  35: app.MapGet("/health", () => Results.Ok(new
```

---

## Predicted Fix *(CodeNarrator — before any fix was applied)*

> _No language model available._

---

## Actual Fix Applied

```diff
diff --git a/IntitechApi.csproj b/IntitechApi.csproj
index 9420abc..003969e 100644
--- a/IntitechApi.csproj
+++ b/IntitechApi.csproj
@@ -1,14 +1,14 @@
 <Project Sdk="Microsoft.NET.Sdk.Web">
 
   <PropertyGroup>
-    <TargetFramework>net9.0</TargetFramework>
+    <TargetFramework>net10.0</TargetFramework>
     <Nullable>enable</Nullable>
     <ImplicitUsings>enable</ImplicitUsings>
     <RootNamespace>IntitechApi</RootNamespace>
   </PropertyGroup>
 
   <ItemGroup>
-    <PackageReference Include="Microsoft.AspNetCore.OpenApi" Version="9.0.0" />
+    <PackageReference Include="Microsoft.AspNetCore.OpenApi" Version="10.0.0" />
   </ItemGroup>
 
 </Project>
diff --git a/Services/GitHubService.cs b/Services/GitHubService.cs
index 245a42a..4444d05 100644
--- a/Services/GitHubService.cs
+++ b/Services/GitHubService.cs
@@ -12,7 +12,7 @@ public class GitHubService(
     IConfiguration config,
     ILogger<GitHubService> logger)
 {
-    private readonly string _username = config["GitHub:Username"] ?? "intitech";
+    private readonly string _username = config["GitHub:Username"] ?? "intisor";
     private readonly string? _token = config["GitHub:Token"];
 
     private void SetAuthHeaders()
diff --git a/appsettings.json b/appsettings.json
index ba89833..0462495 100644
--- a/appsettings.json
+++ b/appsettings.json
@@ -7,7 +7,7 @@
   },
   "AllowedHosts": "*",
   "GitHub": {
-    "Username": "intitech",
+    "Username": "intisor",
     "Token": ""
   }
 }

```

---

## Validation

> _No language model available for validation._

---

*Generated by [CodeNarrator](https://github.com/codenarrator/codebase-narrator) · 2026-04-29 21:28:39 UTC*
